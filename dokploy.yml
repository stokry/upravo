app: upravo-portal
services:
  - name: web
    dockerfile: Dockerfile
    port: 5173
    env:
      - NODE_ENV=production
    healthcheck:
      path: /
      interval: 10s
      timeout: 5s
      retries: 3
    autoscaling:
      min: 1
      max: 2
    resources:
      cpu: 0.5
      memory: 1Gi